"""
GraphRAG 프롬프트 (한국어).

- 엔티티 추출: 청크에서 중요한 개념·기준·도구·사원 등을 뽑아냄
- 관계 추출:   엔티티 간 관계 (정의·조건·통합 등)
- 요약:        커뮤니티(그래프 군집) 요약
- 답변:        검색 결과를 종합한 자연어 답변

nano-graphrag 는 영어 기본 프롬프트라 한국어로 교체해 품질 향상.
"""

ENTITY_EXTRACTION_PROMPT = """다음 텍스트에서 중요한 엔티티(개념·기준·도구·기관 등)를 추출하라.

# 추출 규칙
- 엔티티 타입: CONCEPT(개념), CRITERIA(기준), TOOL(분석 도구), GRADE(등급), DEPT(부서), METRIC(지표)
- 각 엔티티는 한 줄에 "이름 | 타입 | 한 줄 설명" 형식
- 일반 단어("사람", "회사") 같은 모호한 건 제외
- HR 분석 도메인에 특화된 것만

# 텍스트
{text}

# 추출 결과 (한 줄당 하나):"""


RELATION_EXTRACTION_PROMPT = """다음 텍스트와 엔티티 목록에서 엔티티 간 관계를 추출하라.

# 관계 타입
- 정의된다 (X 는 Y 의 정의다)
- 조건이다 (X 는 Y 의 조건이다)
- 통합한다 (X 는 Y 를 통합한다)
- 활용한다 (X 는 Y 를 활용한다)
- 연관된다 (X 는 Y 와 연관된다)
- 영향준다 (X 는 Y 에 영향을 준다)
- 비교된다 (X 와 Y 는 비교 가능하다)

# 형식
한 줄에 "엔티티A | 관계 | 엔티티B | 근거 한 줄"

# 엔티티 목록
{entities}

# 텍스트
{text}

# 추출 결과:"""


COMMUNITY_SUMMARY_PROMPT = """다음 엔티티·관계 군집을 한 단락으로 요약하라.

# 군집의 엔티티
{entities}

# 군집의 관계
{relationships}

# 요약 지침
- 이 군집이 무엇에 대한 영역인지 (예: "등급 체계", "보상 관리")
- 핵심 개념들과 연결
- 5줄 이내, 자연스러운 한국어

요약:"""


ANSWER_GENERATION_PROMPT = """다음 검색 결과를 활용해 사용자 질문에 답하라.

# 질문
{query}

# 관련 청크
{chunks}

# 관련 엔티티·관계
{graph_context}

# 답변 지침
- 청크와 관계 정보를 종합해서 답
- "X 와 Y 의 관계는 ..." 같은 관계 질문엔 그래프 관계 적극 활용
- "전체 ..." 같은 요약 질문엔 커뮤니티 요약 활용
- 단정적으로 답하되, 출처가 불명확하면 "문서에 명시되지 않음" 표시
- 8줄 이내, markdown 헤더 금지

답변:"""
